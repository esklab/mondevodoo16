version: '3.2'
services:
  web:
    image: odoo:16.0
    depends_on:
      - db
    ports:
      - "8067:8069"
    volumes:
      - ./addons:/mnt/extra-addons
    environment:
      - AUTO_INSTALL="odoo,accounting"
  db:
    image: postgres:15
    environment:
      - POSTGRES_DB=postgres
      - POSTGRES_PASSWORD=odoo
      - POSTGRES_USER=odoo
